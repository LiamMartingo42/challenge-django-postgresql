# challenge-django-postgresql
---
Only command to test with docker compose in your machine:
```Makefile
    make compose
```
## *If doesn´t work*
try with :
```Makefile
    make build
    make migrate
    make run
```
